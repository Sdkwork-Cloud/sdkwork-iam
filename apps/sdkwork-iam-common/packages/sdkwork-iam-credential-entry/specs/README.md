# IAM Credential Entry Component Specs

`component.spec.json` declares the public package boundary for credential-entry bootstrap transport, environment-aware Node orchestration, and the Vite development handoff.

Canonical behavior remains owned by `sdkwork-specs/IAM_CREDENTIAL_ENTRY_SPEC.md`, `ENVIRONMENT_SPEC.md`, and `CONFIG_SPEC.md`.
