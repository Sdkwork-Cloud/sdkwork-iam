/// IAM Flutter mobile auth route manifest aligned with H5/PC auth paths.
class IamFlutterMobileAuthRoutes {
  const IamFlutterMobileAuthRoutes._();

  static const moduleId = 'iam-flutter-mobile-auth';
  static const loginPath = '/auth/login';
}
