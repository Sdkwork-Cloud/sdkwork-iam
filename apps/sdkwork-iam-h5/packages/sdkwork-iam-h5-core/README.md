# sdkwork-iam-h5-core

H5 dependency composition entry for IAM mobile React modules.

Re-exports auth runtime discovery (`resolveSdkworkAuthRuntimeConfigFromMetadata` and related types) from `@sdkwork/iam-contracts` per `IAM_OAUTH_SPEC.md` §5.
