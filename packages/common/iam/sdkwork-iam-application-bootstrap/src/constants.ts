/** Canonical IAM bootstrap subject defaults — keep aligned with `bootstrap_subject.rs`. */
export const DEFAULT_IAM_TENANT_ID = "100001";
export const DEFAULT_IAM_TENANT_CODE = "SDKWORK";
export const DEFAULT_IAM_ORGANIZATION_ID = "0";
