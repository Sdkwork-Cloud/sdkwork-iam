# Governance Node Tests

Purpose: static governance, extraction boundary, runner contract, and workspace
standard tests executed by `scripts/run-appbase-governance-node-tests.mjs`.

Owner: `sdkwork-iam` maintainers.

## Catalog

The governed test surface is listed in `scripts/appbase-governance-node-test-catalog.mjs`.
Shared SDK family OpenAPI authority paths live in `appbase-sdk-family-surfaces.mjs`.

## Verification

- `pnpm run test:governance-node`
